# Archived Shell Scripts

These scripts are preserved as historical records of past experiments. They
contain hardcoded paths from the original training server and will **not** run
without modification.

For runnable scripts, see `shell/active/`.
