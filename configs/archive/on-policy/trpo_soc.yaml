algo: TRPO
env_id: CityLearnSafety-SoC-v0


train_cfgs:
  total_steps: 87590
  vector_env_nums: 1

algo_cfgs:
  steps_per_epoch: 8759
  obs_normalize: true
  reward_normalize: true
  # try cost_normalize only if your build supports it; remove if it errors
  cost_normalize: true

logger_cfgs:
  use_tensorboard: true
  use_wandb: false
